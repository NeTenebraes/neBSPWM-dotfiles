conky.config = {
    lua_load = os.getenv("HOME") .. '/.config/conky/lua/storage.lua',
    lua_draw_hook_pre = 'conky_storage_main',
    own_window = true,
    own_window_class = 'Conky',
    own_window_type = 'desktop',
    own_window_transparent = true,
    own_window_argb_visual = true,
    own_window_argb_value = 0,
    own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',
    alignment = 'bottom_right',
    gap_x = 10, 
    gap_y = 10,
    minimum_width = 500, 
    minimum_height = 150,
    update_interval = 2.0, 
    double_buffer = true,
};
conky.text = [[ ]];